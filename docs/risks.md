# Risk register

| ID | Risk / trigger | Mitigation and fallback | Owner |
|---|---|---|---|
| R1 | WSLg rendering unstable or crashes | Keep Gazebo server and benchmarks headless; inspect bridged images/RViz separately | T10/T20 |
| R2 | ROS/Gazebo/MoveIt package mismatch | Pin apt/package versions after first green smoke test; no mid-project upgrades | T10 |
| R3 | Dataset class IDs, split assumptions, materialized contents, or actual model-input directories differ from the accepted manifest | Verify the pinned size and MD5 before safe extraction; inspect the archive layout; refuse ambiguous numeric sequences without an authoritative scene-group map; bind every file to content hashes and path-independent canonical split/dataset digests; reject unregistered/wrong-directory inputs; update the manifest by ADR before training if source facts differ | T30 |
| R4 | The real-image detector remains below the original T30 gate, and the rejected simulator-adaptation candidate also regressed real validation | ADR 0026 accepts only the clean-label `best.pt` (macro/ripe/unripe F1 0.800675/0.887064/0.714286) for bounded simulator engineering at threshold 0.58. Preserve the failed metric, keep the real test sealed, and label every downstream result `ACCEPTED_WITH_WAIVER`; do not substitute the rejected simulator-adaptation candidate or claim sim-to-real success | T30/T60/T70 |
| R5 | Bounding-box centre depth hits leaves/background, visible-surface depth is mistaken for fruit-centre depth, detector latency pairs a box with an unrelated newer frame, or inward mesh winding exposes the rear depth surface | Median central crop, valid-pixel threshold, sigma gate, three-frame retry, and a count/time-bounded timestamp cache with coherent frame/dimension checks. Derive the camera-ray surface-to-centre offset from the selected scene contract: 35 mm for v1 and 26 mm for v2; reject mismatched overrides. ADR 0040 repairs the inward v2 body winding, and ADR 0042 records the camera-clear 100-position pass at 4.503614/5.045998 mm median/P95. Keep natural-plant occlusion as a separate multi-view gate | T40 |
| R6 | Module metrics pass but end-to-end fails | Keep oracle path and first-failing-stage logs to isolate perception/localization/planning | T60/T70 |
| R7 | Gazebo friction makes grasps nondeterministic | Require dual contact then use a temporary attach constraint; report this abstraction | T20/T50 |
| R8 | Scope expansion threatens deadline | P4 permits only one measured bottleneck improvement; P6 is defect-only | T00 |
| R9 | The arm controller can report a Cartesian segment complete while the measured hand pose remains outside tolerance | Keep the 10 mm post-execution verification fail-closed. Allow exactly one correction from the measured pose, never a tolerance relaxation or loop. T60 v1 failed 2/10; v2 passed 10/10 and one 11.6 mm miss was corrected to 0 mm. Continue tracking correction counts in T70 | T50/T60/T70 |
| R10 | Resolved: the source archive and pinned YOLO weights are now present and hash-verified | Keep the exact local files immutable; every training/evaluation preflight rechecks their identities and the materialized split | T30 |
| R11 | Validation labels were incomplete, and the approved model-screened correction still leaves T30 below its gate | ADR 0007 conservatively excludes all 8 disagreements. The approved 13-box derivative and 26-checkpoint sweep reach only 0.815818 macro-F1. ADR 0009's single unripe-x2 exposure run regressed to 0.795696 and increased unripe low-confidence false negatives from 15 to 29. Preserve the test seal, do not reopen exclusions based on F1, and do not authorize another training change without a new evidence-backed decision. Because candidates were model-screened, treat the audited result as validation evidence, not independent generalization | T30/T00 |
| R12 | Multi-fruit outcome depends on relative neighbor/hand-path geometry, not only fruit distance | ADR 0012's paired v2 run is valid in 5/5 and succeeds 4/5. The 64.6 mm layout repeatedly collides `panda_hand` with `strawberry_fruit_2`, while the differently aligned 30 mm layout succeeds. ADR 0013 prohibits a scalar clearance threshold. Preserve the fail-closed collision and defer any approach optimization until the P4 bottleneck is selected | T50/T60/T70 |
| R13 | Simulator truth used to synchronize dynamic MoveIt obstacles could be mistaken for perception evidence | The truth snapshot is diagnostic-only, must be fresh/complete, and may update collision geometry only. It cannot select the target or replace the action target pose. Live fruit truth is not authorized for formal perception-controlled P3/P4 without a separate decision | T00/T50/T60 |
| R14 | Gazebo pose-control bridge can accept service readiness but fail to answer an individual `set_pose` call | Keep a maximum of two five-second attempts, remove a timed-out pending request when supported, and record per-model attempt counts. Classify exhaustion as infrastructure failure, never as a manipulation collision. The authoritative clearance v2 configured all 15 poses on the first attempt | T20/T60 |
| R15 | A robustness matrix could appear complete when lighting/occlusion injection is unchanged, or an absolute blue-pixel check could confuse the existing collection-bin background with the injected occluder | ADR 0014 requires hash-bound worlds and camera measurements. ADR 0017 tracks the visual-only occluder across positions. ADR 0018 preserves the near-left absolute-blue failure and verifies no-occlusion structurally plus heavy coverage relative to each position's stable background. Never treat an injection-check pass as model or P4 acceptance | T20/T70 |
| R16 | Shadow detection totals can be mistaken for condition-normalized accuracy even though observation starts before scene parking and trial durations differ | Treat the ADR 0015 totals as channel-liveness evidence only. Before comparing perception across conditions, reset counters after all pose acknowledgements and use a fixed number of settled frames per scenario. Do not infer recall or degradation from lifetime message totals | T30/T70 |
| R17 | Simplified fruit visuals may distort simulator perception evidence, while a syntactically valid texture or mesh may still render as a flat color or white object | ADR 0023 requires renderer screenshots before model interpretation. The valid v4 A/B/C test shows shape+calyx raises ripe confidence by 0.097551, texture alone has no material effect, and all variants still miss unripe fruit. Keep the canonical world unchanged until the v4 asset passes a separate Oracle-motion regression; never treat v1/v2/v3 renderer failures as model evidence | T20/T30/T70 |
| R18 | Model-screened training-label corrections can reinforce model bias and did not resolve the original T30 metric | ADR 0025 applied only 13 double-review consensus changes and excluded all 16 disagreements. ADR 0026 does not reinterpret the resulting 0.800675/0.887064/0.714286 scores; it permits the exact model only as an engineering-waived simulator controller. Preserve original labels, the test seal, and the distinction between numerical failure and operational waiver | T30/T00 |
| R19 | A repeated one-pose development pass could be mistaken for formal end-to-end or robustness acceptance | ADR 0027's 10/10 positive and 10/10 negative result authorizes only the next P3 engineering step. Keep `formal_acceptance=false`, preserve the failed T30 numeric gate and test seal, and require a separately frozen 135+30 contract before any formal P3 claim | T00/T60/T70 |
| R20 | Formal seed labels could be reported as three random trials without affecting Gazebo | ADR 0029 adds an opt-in `simulation_seed` launch path and binds each formal scenario seed to `gz sim --seed`. Keep the condition materializer ID separate and never call it an independent random seed | T00/T20/T70 |
| R21 | Waiting for a positive YOLO target before starting the formal state machine could hide a missed detection as infrastructure failure | The authoritative v2 preflight requires ten settled detection frames, then starts the trial even with no target. Positive `NO_PICK` is attributed to perception and is not infrastructure-retried. The contract rejects the superseded v1 preflight path | T00/T60/T70 |
| R22 | The consumed formal P3 matrix fails at 39/135 positives, and selective reruns or multiple simultaneous fixes could hide the measured bottleneck | ADR 0030 makes all 165 one-attempt outcomes immutable. Preserve the passed 30/30 negative safety subgate and failed overall gate separately. P4 must freeze exactly one perception-focused intervention and a separately named evaluation before changing model, threshold, asset, or runtime behavior; retain the 12 grasp failures as a secondary issue | T00/T30/T70 |
| R23 | A simulator-adapted detector can recover occluded classifications while box-centre depth still localizes the foreground occluder | ADR 0032 preserves 300/300 heavy ripe detections but 0/300 heavy target poses. Keep the 0.080 m truth-association guard fail-closed; do not relax it, substitute truth, or claim the detector alone repairs the end-to-end chain. Treat occlusion-aware depth/segmentation or multi-view localization as future work outside the consumed one-intervention P4 scope | T40/T70/T80 |
| R24 | The canonical Blender-v2 unripe fruit remains visually confusable with leaves even when isolated | ADR 0037's single 12-epoch adaptation claim is consumed and ADR 0038 rejects it: fixed `last.pt@0.58` reaches ripe F1 0.857143 but unripe F1 0 on the disjoint synthetic held-out split. Keep the prior engineering checkpoint active. Any closer-crop/object-scale or class-aware-sampling study requires a new decision, dataset, non-regression budget, and one-time claim | T30/T70 |
| R25 | Reusing v1 gripper close and tool offsets with the smaller 26 mm v2 collision sphere could miss dual contact even when planning succeeds | ADRs 0043-0047 replace the mismatch with an exact scene-bound profile and pass the frozen mesh sweep, bilateral gripper-only contact/attach/detach/recovery round trip, and controller-free pre-grasp planning gate. Keep final approach, target-collision removal, arm execution, retreat, placement, repeated picks, and perception-derived picking unauthorized until separately frozen execution gates pass | T50/T60 |
| R26 | Blender OBJ vertex normals point outward while fruit-body face winding points inward, causing the RGB-D camera to record the rear surface and add a systematic camera-ray error | Preserve the failed 44 mm pre-repair gate, mechanically reverse only the 11,796 body face references in each ripe/unripe mesh, bind before/after hashes, assert outward winding in tests, inspect both rendered classes, and retain the 100-position post-repair qualification | T40/T70 |
